{
  "name": "muse-contracts",
  "version": "1.0.0",
  "description": "Muse Smart Contracts - AI Art Marketplace on Stellar",
  "scripts": {
    "build": "stellar contract build",
    "test": "stellar contract test",
    "deploy:standalone": "stellar contract deploy --network standalone",
    "deploy:testnet": "stellar contract deploy --network testnet",
    "deploy:mainnet": "stellar contract deploy --network mainnet",
    "optimize": "stellar contract optimize",
    "clean": "stellar contract clean"
  },
  "devDependencies": {
    "@stellar/stellar-sdk": "^11.0.0",
    "@stellar/soroban-cli": "^1.0.0",
    "@types/node": "^20.5.0",
    "typescript": "^5.1.6",
    "ts-node": "^10.9.1",
    "jest": "^29.6.2",
    "@types/jest": "^29.5.4"
  },
  "dependencies": {
    "stellar-contracts": "^0.8.0"
  }
}
